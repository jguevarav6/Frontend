export const environment = {
  production: true,
  // URL base de la API para producción (puede cambiarse si se despliega con
  // un backend específico).
  apiBaseUrl: 'https://jsonplaceholder.typicode.com'
};
